name: ViteEtGourmand Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: linux

    steps:
      - name: Update repo
        run: |
          git config --global --add safe.directory /opt/apps/ViteEtGourmand
          cd /opt/apps/ViteEtGourmand
          git pull origin main

      - name: Build image
        run: |
          cd /opt/apps/ViteEtGourmand
          docker compose build

      - name: Deploy
        run: |
          cd /opt/apps/ViteEtGourmand
          docker compose up -d
